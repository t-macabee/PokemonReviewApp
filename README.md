# PokemonReviewApp

Simple app demostrating .NET API basics, with focus on table relationships.


Seed files are present inside the project and they have to be manually executed using 'dotnet run seeddata' command inside terminal.

